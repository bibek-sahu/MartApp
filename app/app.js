var module,
    _modules = [
        "ngNewRouter",
        "MartApp.navigation",
        "MartApp.home",
        "MartApp.footer",
        "MartApp.about"
    ];

module = angular.module("MartApp", _modules);